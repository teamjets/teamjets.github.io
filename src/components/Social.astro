---
const { icon, website, username = "", platform } = Astro.props;
---

<div>
  <img class="social-icons" src={`${icon.src}`} alt={`${website} icon`} />
  <a class="social-links" href={`${website}/${username}`} target="_blank">
    {platform}&nbsp;&nbsp;
  </a>
</div>
